# CLAUDE.md

@AGENTS.md

## Agent skills

### Issue tracker

작업 이슈와 PRD는 `pwh9882/dotfiles`의 GitHub Issues에서 관리한다. 자세한 명령과 규칙은 `docs/agents/issue-tracker.md`를 따른다.

### Triage labels

`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`를 표준 triage label로 사용한다. 역할 매핑은 `docs/agents/triage-labels.md`에 있다.

### Domain docs

이 저장소는 루트 `CONTEXT.md`와 `docs/adr/`를 사용하는 single-context 구조다. 문서 탐색 규칙은 `docs/agents/domain.md`를 따른다.
